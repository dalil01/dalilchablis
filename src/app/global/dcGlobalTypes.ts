export type dcPosition = { x: number, y: number };

export type dcDimension = { w: number, h: number };

export type dcCoordinates = dcPosition & dcDimension;

export type dcData = { EN: any, FR: any };
